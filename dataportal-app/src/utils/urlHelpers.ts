// This file is kept for backward compatibility
// The main URL utilities are now in urlSync.ts

// Re-export from urlSync to maintain backward compatibility
export { buildShareableUrl, parseUrlFilters } from './urlSync';
