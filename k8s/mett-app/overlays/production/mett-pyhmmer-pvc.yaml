apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mett-faa-pvc
  namespace: mett-dataportal-hl-dev
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: local-storage
  resources:
    requests:
      storage: 12Gi
